import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Park City Angels | Backing Exceptional Early-Stage Founders",
  description:
    "Park City Angels backs exceptional early-stage founders across the United States with capital, operator perspective, and long-term partnership.",
  icons: {
    icon: "/pca_logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
